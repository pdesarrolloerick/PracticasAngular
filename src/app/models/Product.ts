// Interfaz o modelo para un objeto producto que se usara en la componente models-interfaces

// *! Para poder acceder debes escribir: export default

// Ejemplo: 
export default interface Products {
    name: string,
    price: number,
    isForSale: boolean,
}